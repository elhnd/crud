{% extends 'base.html.twig' %}

{% block title %}Hello BlogController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code>/home/elhadji/certif/crud/src/Controller/BlogController.php</code></li>
        <li>Your template at <code>/home/elhadji/certif/crud/templates/blog/index.html.twig</code></li>
    </ul>

    {% for i in 0..10 %}
        * {{i}}
         {# {{ loop.parent }} - #}
    {% endfor %}
    <br>
    {% for i in range(50, 60) %}
        * {{loop.index0}} -
        * {{i}}
    {% endfor %}
    <br>
    {% for letter in 'a'..'z' %}
        * {{loop.revindex0}} -
        * {{ letter }}
    {% endfor %}
    <br>
    {% for letter in 'a'|upper..'z'|upper %}
        {% if loop.first %}
            {{ 'This is the first letter: ' ~ letter }} <br>
        {% endif %}

        {% if loop.last %}
            This is the last letter: 
        {% endif %}
        * {{ letter }}
    {% endfor %}
    <br>

    {# <h1>Members</h1>
    <ul>
        {% for key in users|keys %}
            <li>{{ key }}</li>
        {% endfor %}
    </ul>
    <ul>
    {% for key, user in users %}
        <li>{{ key }}: {{ user.name|e }}</li>
    {% endfor %} #}
</ul>
{# {{ include('blog/_user_profile.html.twig', {'users': users}) }} #}

<h1>Members</h1>
 {# {{ render(path('blog_users')) }} #}
  {# {{ render(url('blog_users')) }} #}
  {{ render(controller(
        'App\\Controller\\BlogController::users'
    )) }}
</div>
{% endblock %}
